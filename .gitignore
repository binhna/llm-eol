# Service account credentials — never commit these
credentials.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Environment
.env

# Private bare mirrors of the product repos, rebuilt on demand.
# Never contains anyone's working files — history only, no working tree.
.cache/
